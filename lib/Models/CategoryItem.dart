class CategoryItem {
  CategoryItem(this.categoryName, this.categoryDescription, this.categoryIcon, this.categoryID);

  String categoryName;
  String categoryDescription;
  String categoryIcon;
  int categoryID;
}
